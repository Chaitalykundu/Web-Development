1. Images were not loading

Check your `angular.json` configuration file to ensure the assets folder is being included in the build. You should see something like this:

```json
"assets": [
  "src/favicon.ico",
  "src/assets"
]
```

&nbsp;

&nbsp;
